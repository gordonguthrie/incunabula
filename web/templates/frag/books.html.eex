<%= if @books != [] do %>
<table class="ui striped table">
  <%= for {title, slug} <- @books do %>
    <tr>
      <td>
        <a href="<%= "/books/" <> slug %>" alt="<%= title %>"><%= title %></a>
      </td>
    </tr>
    <% end %>
</table>
<% end %>
